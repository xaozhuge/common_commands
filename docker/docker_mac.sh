# 1. mac 命令行退出 docker
osascript -e 'quit app "Docker"'

# 2. mac 命令行启动 docker
open -a Docker

# 3. 通过 AppleScript 让 macOS 系统退出名为 “Docker” 的应用程序, 也就是关闭 Docker Desktop
osascript -e 'quit app "Docker"'


# 1. gitlab-runner 注册, 标签和描述可以使用 server(ip)
gitlab-runner register

# 2. gitlab-runner 重启
gitlab-runner restart

# 3. gitlab-runner 列表
gitlab-runner list

# 4. 移除 gitlab-runner 
sudo vi /etc/gitlab-runner/config.toml
